import React from 'react';
import Layout from '../components/layout';

export const Home = () => {
    return (
        <div>
            <Layout>
                <main></main>
            </Layout>
        </div>
    );
};

export default Home;
