import React from 'react';
import Layout from '../components/layout';
import Products from './Products';

export const Home = () => {
    return (
        <div>
            <Layout>
                <section id="home">
                    <h1>Home</h1>
                </section>
            </Layout>
        </div>
    );
};

export default Home;
