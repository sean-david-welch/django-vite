import React from 'react';
import Layout from '../components/layout';

export const Cart = () => {
    return (
        <Layout>
            <section id="cart">
                <h1>Cart</h1>
            </section>
        </Layout>
    );
};

export default Cart;
