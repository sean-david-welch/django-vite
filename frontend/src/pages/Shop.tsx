import Layout from '../components/Layout';
import Products from '../components/Products';

export const Shop = () => {
    return (
        <Layout>
            <Products />
        </Layout>
    );
};

export default Shop;
