import Layout from '../components/Layout';
import ProductsList from '../components/products/ProductsList';

export const Shop = () => {
    return (
        <Layout>
            <ProductsList />
        </Layout>
    );
};

export default Shop;
