import React from 'react';
import Layout from '../components/layout';

export const Login = () => {
    return (
        <Layout>
            <section id="login">
                <h1>Login</h1>
            </section>
        </Layout>
    );
};

export default Login;
