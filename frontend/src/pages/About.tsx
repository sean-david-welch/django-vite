import React from 'react';
import Layout from '../components/layout';

export const About = () => {
    return (
        <Layout>
            <section id="about">
                <h1>About</h1>
            </section>
        </Layout>
    );
};

export default About;
