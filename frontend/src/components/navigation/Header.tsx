import Navbar from './Navbar';
import SideNavbar from './SideNavbar';

function Header() {
    return (
        <header>
            <Navbar />
            <SideNavbar />
        </header>
    );
}

export default Header;
